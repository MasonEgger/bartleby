  1. End-to-end smoke test (highest ROI). Run bartleby new site demo && cd demo && bartleby new post "Hello" --type blog && bartleby build, then open demo/site/index.html in a browser. If the homepage renders, the blog post renders, the search index loads, and    
  feeds/sitemap are valid, the integration story holds together. The 272 passing tests prove the pieces work in isolation; this proves they work together.                                                                                                              
                                                                                                                                                                                                                                                                        
  2. Audit the deferrals. I explicitly flagged shortcuts in session summaries — most importantly:                                                                                                                                                                       
  - Step 25 (theme): real Tailwind compilation skipped — main.css is hand-written CSS, not generated. Decide whether that ships as-is or you wire up the standalone Tailwind CLI.                                                                                       
  - Step 24 (vendor JS): alpine.min.js, htmx.min.js, lunr.min.js are stubs that just console.debug. Nothing actually runs client-side. Replace with real bundles before any visible release.                                                                            
  - Step 26 (icons): one sample SVG per pack. Real pack vendoring still TODO.                                                                                                                                                                                           
  - Step 27 (async): async_build is just asyncio.to_thread(build, ...) — no real CPU parallelism. Fine for now, but you set up the API; you'd want to revisit if build times bite.                                                                                      
  - The pre-existing uncommitted extra_css/extra_js config diff and the spec/plan refinement diff from session start were never folded in — they're still in your working tree.                                                                                         
                                                                                                                                                                                                                                                                        
  3. Test quality vs. test count. Sample a few test files and ask "does this test the behavior or just the shape?" The honest answer:                                                                                                                                   
  - Strong: test_build.py, test_async_build.py, test_cli.py exercise real end-to-end paths.                                                                                                                                                                             
  - Mixed: theme tests check for strings in HTML/CSS ("admonition" in css) — verifies the rule exists, not that it renders correctly in a browser.                                                                                                                      
  - Weak: test_server.py skips real socket/watcher work; test_async_build.py doesn't verify actual concurrency, only that the coroutine completes.                                                                                                                      
                                                                                                                                                                                                                                                                        
  4. Read the commits as a story. git log --oneline main..v1 gives you 27 commits, each ~self-contained. Skim the diffs for Steps 4 (parse_front_matter had a bug-fix loop), 8 (template cascade — most architecturally load-bearing), 10 (MVP build pipeline — wires   
  everything), and 21 (plugin system — easy to mess up). Those are the ones where design errors compound downstream.                                                                                                                                                    
                                                                                                                                                                                                                                                                        
  5. Lint the spec against the implementation. spec.md was the canonical source of truth. A focused diff between "what spec.md promised" and "what got built" would surface gaps. The plan covered 27 of 36 numbered steps mentioned in spec — Phase 5 "Agent           
  Integration" (structured CLI output, schema introspection, skill generation, etc.) was scoped out of this todo entirely.                                                                                                                                              
                                                                                                                                                                                                                                                                        
  6. Run /ultrareview on the branch. It'll spawn parallel cloud reviewers across the diff. It's billed and user-triggered, so I can't kick it off, but for 27 commits of unsupervised AI work it's probably the single best independent check. 
