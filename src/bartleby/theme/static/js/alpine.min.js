/* ABOUTME: Vendored Alpine.js stub — replaced with real bundle in Step 25.
   This placeholder lets the build pipeline emit a script reference now. */
console.debug && console.debug("alpine.js placeholder");
